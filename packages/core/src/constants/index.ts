import type { SchedulerPriority } from '../types/Scheduler';

export const MIN_SCHEDULER_PRIORITY = 0 satisfies SchedulerPriority;
export const MAX_SCHEDULER_PRIORITY = 7 satisfies SchedulerPriority;
